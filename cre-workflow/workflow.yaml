# ==========================================================================
# CRE WORKFLOW SETTINGS FILE (Chainlink Confidential Bootcamp Standard)
# ==========================================================================
staging-settings:
  user-workflow:
    workflow-name: "aquaghost-sentinel-staging"
    deployment-registry: "private"
  workflow-artifacts:
    workflow-path: "./src/main.ts"
    config-path: "./config.staging.json"
    secrets-path: "./secrets.yaml"

# ==========================================================================
production-settings:
  user-workflow:
    workflow-name: "aquaghost-sentinel-production"
    deployment-registry: "private"
  workflow-artifacts:
    workflow-path: "./src/main.ts"
    config-path: "./config.production.json"
    secrets-path: "./secrets.yaml"
